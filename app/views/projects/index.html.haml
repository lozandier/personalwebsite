%h1= "I have #{pluralize(@projects.size, 'project')}"

- @projects.each do |project| 
  %div.project 
    =image_tag project.main_image.url
    %h2= link_to project.title, project_path(project.project)
  %div.project
    
    
